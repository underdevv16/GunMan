Trial readme. 

This is a game I'll develop as a project to learn more about the C language.

Using x64 native tools cmd for vs 2022
to build the project, use the command: build && GunMan -- after going in the project directory.
